-- // ULTIMATE HUB v9 - CINNAMON STYLE //
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local hijackActive = false
local flying = false
local invisible = false
local walkSpeedBoost = 50
local flySpeed = 90

local hijackConnection, hijackVelocity, hijackGyro = nil, nil, nil
local speedConnection = nil

local CarsFolder = workspace:FindFirstChild("Cars") or workspace:FindFirstChild("cars") or workspace

-- ====================== MAIN GUI ======================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CinnamonHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 820, 0, 520)
MainFrame.Position = UDim2.new(0.5, -410, 0.5, -260)
MainFrame.BackgroundColor3 = Color3.fromRGB(13, 13, 17)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

-- Header
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 50)
Header.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
Header.Parent = MainFrame
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0.5, 0, 1, 0)
Title.Position = UDim2.new(0, 20, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "Axonic / Cinnamon"
Title.TextColor3 = Color3.fromRGB(0, 255, 180)
Title.TextSize = 18
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Header

-- Sidebar
local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Size = UDim2.new(0, 180, 1, -50)
Sidebar.Position = UDim2.new(0, 0, 0, 50)
Sidebar.BackgroundColor3 = Color3.fromRGB(16, 16, 22)
Sidebar.ScrollBarThickness = 4
Sidebar.Parent = MainFrame

local tabButtons = {}
local tabs = {
    {name = "Car Jacker", icon = "🚗"},
    {name = "Movement", icon = "⚡"},
    {name = "Combat", icon = "⚔️"},
    {name = "Visuals", icon = "👁️"},
    {name = "Misc", icon = "🔧"}
}

for i, tab in ipairs(tabs) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 50)
    btn.Position = UDim2.new(0, 10, 0, 15 + (i-1)*60)
    btn.BackgroundColor3 = i == 1 and Color3.fromRGB(0, 100, 255) or Color3.fromRGB(25, 25, 35)
    btn.Text = tab.icon .. "  " .. tab.name
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 15
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = Sidebar
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    
    table.insert(tabButtons, btn)
    
    btn.MouseButton1Click:Connect(function()
        for _, b in pairs(tabButtons) do b.BackgroundColor3 = Color3.fromRGB(25, 25, 35) end
        btn.BackgroundColor3 = Color3.fromRGB(0, 100, 255)
    end)
end

-- Content Area
local Content = Instance.new("Frame")
Content.Size = UDim2.new(1, -190, 1, -60)
Content.Position = UDim2.new(0, 190, 0, 60)
Content.BackgroundTransparency = 1
Content.Parent = MainFrame

-- Пример панели Car Jacker
local function CreateCarJackPanel()
    local panel = Instance.new("Frame")
    panel.Size = UDim2.new(1, 0, 1, 0)
    panel.BackgroundTransparency = 1
    panel.Parent = Content

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 40)
    title.Text = "Car Jacker"
    title.TextColor3 = Color3.fromRGB(0, 255, 180)
    title.TextSize = 18
    title.Font = Enum.Font.GothamBold
    title.BackgroundTransparency = 1
    title.Parent = panel

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.6, 0, 0, 50)
    btn.Position = UDim2.new(0.2, 0, 0.2, 0)
    btn.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
    btn.Text = "🚗 Перехватить руль"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 16
    btn.Font = Enum.Font.GothamBold
    btn.Parent = panel
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    btn.MouseButton1Click:Connect(function() 
        -- вызов функции угна
    end)
end

CreateCarJackPanel()

-- ====================== WalkSpeed Boost (в обход Humanoid) ======================
local function StartWalkSpeedBoost()
    if speedConnection then speedConnection:Disconnect() end
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    speedConnection = RunService.Heartbeat:Connect(function()
        local hum = char:FindFirstChild("Humanoid")
        if hum and hum.MoveDirection.Magnitude > 0 then
            root.AssemblyLinearVelocity = root.AssemblyLinearVelocity + hum.MoveDirection * walkSpeedBoost
        end
    end)
end

-- Закрытие
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -40, 0, 10)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 70, 70)
CloseBtn.TextSize = 20
CloseBtn.Parent = MainFrame
CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

print("Cinnamon Style Hub загружен!")
